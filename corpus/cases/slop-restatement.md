# Caching improves performance

Caching improves performance. Caching is a technique that improves the performance of a
system by improving how often the system has to do expensive work.

Caching improves performance because caching means the system does not repeat expensive
work. Performance improves when work is not repeated.

The performance improvement from caching is a performance improvement that comes from
caching expensive work.
